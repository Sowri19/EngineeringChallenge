import styled from "styled-components/native";

const Container = styled.View`
  padding: 10px;
  margin-bottom: 10px;
  background-color: #f0f0f0;
  border-radius: 8px;
`;

const Title = styled.Text`
  font-size: 20px;
  font-weight: bold;
  color: #1e90ff;
  margin-bottom: 5px;
`;

const PartText = styled.Text`
  font-size: 16px;
  color: #4a4a4a;
`;

export { Container, Title, PartText };
