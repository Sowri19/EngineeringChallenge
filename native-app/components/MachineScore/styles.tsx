import styled from "styled-components/native";
import { View, Text } from "../Themed";

const MachineScoreContainer = styled(View)`
  margin-top: 10px;
`;

const MachineScoreText = styled(Text)`
  font-size: 18px;
`;

export { MachineScoreContainer, MachineScoreText };
